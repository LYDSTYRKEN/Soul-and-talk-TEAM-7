<Window x:Class="Soul_and_talk.AddCustomerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Register new customer"
        Height="135" Width="348">

    <Grid Margin="10">
        <!-- Hovedgrid: 3 rækker, 2 kolonner -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Row 0: Kunde navn -->
        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Text="Kunde navn:"
                   Margin="0,0,8,0" />
        <TextBox Grid.Row="0"
                 Grid.Column="1"
                 Text="{Binding CustomerName}"
                 Margin="0,0,0,10" />

        <!-- Row 1: Institution -->
        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Text="(Valgfri) tilknyttet institution:"
                   Margin="0,0,10,0" />
        <ComboBox Grid.Row="1"
                  Grid.Column="1"
                  ItemsSource="{Binding Institutions}"
                  DisplayMemberPath="Name"
                  SelectedItem="{Binding SelectedInstitution}"
                  Margin="0,0,0,0" />

        <!-- Row 2: Knapper -->
        <Grid Grid.Row="2"
              Grid.ColumnSpan="2"
              Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1"
                    Content="Gem"
                    Width="80"
                    Margin="0,0,8,0"
                    Command="{Binding Path=SaveCustomerCommand}" CommandParameter="{Binding}"/>

            <Button Grid.Column="2"
                    Content="Annuller"
                    Width="80"
                    Command="{Binding Path=CancelCustomerCommand}" CommandParameter="{Binding}" />
        </Grid>
    </Grid>
</Window>
